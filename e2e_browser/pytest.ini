[pytest]
# Browser E2E runs on the HOST against the live compose stack:
#   python3 -m pytest -c e2e_browser/pytest.ini e2e_browser -q \
#       --screenshot=only-on-failure --output=e2e_browser/screenshots
testpaths = e2e_browser
